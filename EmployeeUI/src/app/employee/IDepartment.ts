export interface EmployeeDetail {
    id: string;       // Primary ID from API
    name: string;     // Name of the employee
    departments: string; // Department information (may need to adjust based on actual API response)
  }
  